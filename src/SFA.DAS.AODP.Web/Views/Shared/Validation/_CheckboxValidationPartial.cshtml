@model string

@if (ViewContext.ModelState.ContainsKey(Model))
{
    <govuk-checkboxes-error-message>@ViewContext.ModelState[Model]?.Errors.FirstOrDefault()?.ErrorMessage</govuk-checkboxes-error-message>
}