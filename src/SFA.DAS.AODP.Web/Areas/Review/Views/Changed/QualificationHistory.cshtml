@{
    ViewData["Title"] = "Qualification History";
}
<h1 class="govuk-heading-xl">Qualification History</h1>
<div class="govuk-width-container">
    <main class="govuk-main-wrapper">
        <div class="govuk-tabs">
            <h2 class="govuk-tabs__title">
                Contents
            </h2>
            <ul class="govuk-tabs__list">
                <li class="govuk-tabs__list-item">
                    <a class="govuk-tabs__tab" asp-controller="changed" asp-action="index">
                        Details
                    </a>
                </li>

                <li class="govuk-tabs__list-item  govuk-tabs__list-item--selected">
                    <a class="govuk-tabs__tab" asp-controller="changed" asp-action="QualificationHistory">
                        History
                    </a>
                </li>

            </ul>
            <div class="govuk-tabs__panel">
        @if (!ViewContext.ModelState.IsValid)
        {
                    <div class="govuk-error-summary" data-module="govuk-error-summary">
                        <div role="alert">
                            <h2 class="govuk-error-summary__title">
                                Validation Error
                            </h2>
                            <div class="govuk-error-summary__body">
                                <ul class="govuk-list govuk-error-summary__list">
                                    <li>
                                @foreach (var ms in ViewContext.ModelState)
                                {
                                    var error = @ms.Value.Errors.FirstOrDefault();
                                    if (error == null) continue;

                                        <li>
                                            <a href="#@ms.Key">@error.ErrorMessage</a>
                                        </li>
                                }
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>
        }
            </div>
        </div>
    </main>

</div>

